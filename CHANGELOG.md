# 更新日志

## v1.0.0 — 首个正式版

**定位**:面向模拟 IC 设计的 gm/ID 数据工作室 —— 一次导入 DC 全参数扫描,
每个参数在自己的独立窗口中绘制,三维曲面按需进入。

### 核心能力

* **自适应解析**:宽表(每 L 一对 X/Y 列)/ 长表(含 L 列)/ 多指标宽表 / 简单两列;
  分隔符、编码、L 单位(m→µm)自动适配;支持量纲不同的多指标共存。
* **数据全驻留、按需绘制**:载入只做解析与索引(114 参数 × 15 L 约 0.2 s),
  双击参数才开窗,参数再多也不卡。
* **每窗口独立约束**:X/L/指标范围、X·L 对数轴、Savitzky-Golay 平滑、
  显示单位(原始 / 工程前缀 / dB)、查表开关+方向+阈值,全部逐窗口独立。
* **3D 按需进入**:控制台不常驻 3D;在 2D 窗口点「三维曲面…」弹出独立 3D 窗口,
  旋转时自动降级面片(约 2× 减面 + 关抗锯齿)保证流畅,松手恢复高精度。
* **图上测量**:可拖动竖线/横线,实时给出与**每条曲线的交点值**(带 L 标签,
  图上标注 + 面板列全);「测斜率(A→B)」自动吸附数据点并给出 Δx/Δy/斜率;
  红色阈值虚线可直接拖动改查表阈值。
* **反向设计查表**:按指标类型自动选方向(增益/fT 用 ≥,Vdsat/Vgs/电流用 ≤),
  输出每个 L 的可行 gm/ID 区间与推荐工作点,并导出 CSV。
* **数据库管理**:常用文件入库(复制副本 + 标签),双击即加载/再双击卸载;
  启动只恢复数据与默认约束,不自动弹图。
* **指标单位适配**:内置指标档案(自增益 / fT / Vdsat / Vgs / Vov / Inor / Id / Cgg / gm / gds…),
  每个指标**固定一个工程前缀**并在全部视图共用同一刻度;容忍 `M0:` 前缀与
  `vdast` 之类常见拼写错误。
* **交互细节**:滚轮悬停数值框不误改(可关);浅色/深色主题;参数可重命名并持久化;
  主界面为可展开/折叠区块,参数树支持"只看某个文件"。

### 性能

* 视图状态指纹一致时跳过重绘(实测无变化刷新 ~2–8 ms vs 强制 ~25–80 ms);
* 缓存分级(改范围只重算曲面,复用清洗结果)、超长曲线显示抽稀、
  参数树勾选只同步状态不重建。

### 测试

* `python -m unittest discover -v` — 8 项(单位刻度一致性、同名文件隔离、
  每视图独立、启动脚本);
* `dev/` 下的离屏 GUI 回归:窗口化架构、反复开窗关窗压测、滚轮防误触、
  旧版 GUI 兼容回归。

### 致谢

* [@Binah-Dev](https://github.com/Binah-Dev) — PR
  [#1](https://github.com/Psi215/gm-id-3d-studio/pull/1):单位刻度一致性、
  同名源文件隔离(`source_path`)、查表方向同步、`run.bat` 解释器查找;
  该 PR 已合入当前架构,并在 `tests/` 中补上对应回归用例。
* 由 [Psi215](https://github.com/Psi215) 与 AI 助手(DeepSeek)协作完成。

### 许可

MIT License。
